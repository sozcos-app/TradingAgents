# Phase 8+9 - AI 分析集成 + 定时任务自动化

---

## 一、AI 分析集成（Phase 8）

> 利用项目已有的 LLM 能力，基于指标数据生成"谎言/机会"分析报告。

### 后端任务

#### 8.1 创建 AI 分析服务 `app/services/market_insight_service.py`

```
MarketInsightService 类

├── analyze_macro_divergence(macro_score, market_data) → str
│   Prompt 模板:
│   "当前宏观评分{score}，等级{level}。
│    但二级市场呈现以下特征：{market_data}。
│    请分析是否存在'虚假繁荣'或'被低估'的信号，
│    输出'谎言'或'机会'判断及理由。"
│
├── analyze_industry_anomaly(industry, score_detail) → str
│   Prompt 模板:
│   "行业{industry}景气评分{score}。
│    五维度指标：{details}。
│    请分析是否存在异常（如评分虚高/被低估），
│    识别潜在风险或机会。"
│
├── analyze_stock_risk(stock, health_report) → str
│   Prompt 模板:
│   "股票{stock}体检评分{score}。
│    以下指标触发预警：{alerts}。
│    请深度分析该股票是否存在:
│    1. 财务造假嫌疑
│    2. 蹭概念讲故事
│    3. 隐藏的供应链风险
│    并给出具体建议。"
│
└── analyze_event_impact(event, context) → str
    Prompt 模板:
    "事件：{event_title}，类型{type}，日期{date}。
     当前宏观环境：{macro_brief}。
     请评估该事件对A股的短期和中期影响，
     推荐关注的板块和操作建议。"
```

#### 8.2 LLM 调用方式

复用项目已有的 LangChain + OpenAI 兼容接口：
```python
from tradingagents.llm_clients import get_llm_client

# 调用用户在系统中配置的模型
llm = get_llm_client()
result = await llm.ainvoke(prompt)
```

支持流式输出（SSE），前端实时展示分析过程。

#### 8.3 API 接口

```
POST /api/calendar/macro/insight                      # 宏观谎言/机会分析
POST /api/calendar/industry/{name}/insight             # 行业异常分析
POST /api/calendar/stock-health/{ts_code}/insight      # 个股风险深度分析
POST /api/calendar/events/{id}/insight                 # 事件影响分析
GET  /api/calendar/insights?type=macro&ref_id=xxx      # 查询历史分析结果
```

### 前端任务

#### 8.4 AI 分析展示组件

```
frontend/src/components/Calendar/
├── AiInsightCard.vue
│   ├── 分析类型标签 (宏观/行业/个股/事件)
│   ├── Markdown 渲染分析结果 (用 marked 库)
│   ├── 谎言/机会高亮标签
│   └── 分析时间
│
└── AiInsightStream.vue
    ├── SSE 流式输出 (逐字显示)
    └── "停止生成" 按钮
```

#### 8.5 各页面集成

在 MacroView、IndustryView、StockHealthView、EventCalendarView 的详情区域
各添加"AI 深度分析"按钮，点击后调用对应接口，结果用 AiInsightCard 展示。

---

## 二、定时任务自动化（Phase 9）

### 后端任务

#### 9.1 注册定时任务

在 `app/services/scheduler_service.py` 中或新建 `app/worker/calendar_scheduler.py` 注册：

```
任务列表:
├── calendar_macro_daily
│   ├── Cron: 每交易日 16:30
│   ├── 执行: 采集日度宏观指标 (DR007、北向资金、汇率)
│   └── 完成后: 自动触发宏观评分计算
│
├── calendar_macro_monthly
│   ├── Cron: 每月 11 日 09:00
│   ├── 执行: 采集月度宏观指标 (PMI、CPI、PPI、M2、社融)
│   └── 完成后: 自动触发宏观评分计算
│
├── calendar_market_daily
│   ├── Cron: 每交易日 15:30
│   ├── 执行: 采集二级市场指标
│   └── 完成后: 运行背离检测
│
├── calendar_industry_weekly
│   ├── Cron: 每周日 10:00
│   ├── 执行: 全行业景气评分
│   └── 结果: 更新行业排名
│
├── calendar_event_daily
│   ├── Cron: 每日 08:00
│   ├── 执行: 检查 RSS 源 + 生成固定日历事件
│   └── 完成后: 事件影响力评估
│
├── calendar_event_reminder
│   ├── Cron: 每日 09:00
│   ├── 执行: 查询未来1~7天的重要事件
│   └── 推送: 通知中心 + WebSocket
│
└── calendar_pbc_daily
    ├── Cron: 每日 10:30
    ├── 执行: 检查央行公开市场操作公告
    └── 有新公告时: 入库 + 推送通知
```

#### 9.2 任务管理 API

```
GET  /api/calendar/tasks                        # 查看所有定时任务状态
POST /api/calendar/tasks/{task_id}/trigger      # 手动触发某个任务
PUT  /api/calendar/tasks/{task_id}/toggle       # 启用/禁用任务
GET  /api/calendar/tasks/{task_id}/logs         # 查看任务执行日志
```

### 前端任务

#### 9.3 定时任务管理面板

在"设置"页面或交易日历页面增加：

```
任务管理卡片 (el-table)
列:
├── 任务名称
├── Cron 表达式
├── 状态 (启用/禁用) → el-switch
├── 最近执行时间
├── 最近执行结果 (成功/失败)
├── 下次执行时间
└── 操作 (手动触发按钮)

点击"手动触发" → 调用 calendarApi.tasks.trigger(id)
```

---

## 三、所需库

全部已有，无需新装。AI 分析复用项目已有的 LangChain + LLM 配置。

---

## 四、Phase 8+9 完成标准

- [ ] 各页面"AI 深度分析"按钮可点击并返回分析结果
- [ ] 分析结果以 Markdown 格式渲染展示
- [ ] 定时任务按计划自动执行
- [ ] 前端可查看任务状态和手动触发
- [ ] 事件提醒推送到通知中心
