# Phase 5 - 行业景气模块

> 目标：计算行业景气评分(0~100)，输出行业排名和配置建议(超配/标配/低配)。

---

## 一、后端任务

### 5.1 创建行业数据采集器 `app/services/industry_fetcher.py`

```
IndustryFetcher 类

├── fetch_inventory_cycle() → list
│   数据源: 国家统计局 / AKShare
│   字段: 分行业产成品库存同比、PPI分行业
│   频率: 月度
│   用途: 判断库存周期阶段
│
├── fetch_industry_profitability() → list
│   数据源: Tushare pro.industry_indicator()
│   字段: 行业毛利率、ROE(TTM)、净利润增速
│   频率: 季度
│   用途: 盈利趋势评分
│
├── fetch_industry_demand() → list
│   数据源:
│   - 汽车销量: AKShare ak.car_sales()
│   - 行业出货量: Tushare pro.disclosure()
│   字段: 销量同比、订单增速
│   频率: 月度
│   用途: 需求增速评分
│
├── fetch_industry_capital_flow() → list
│   数据源:
│   - 行业成交额: AKShare ak.stock_board_industry_name_em()
│   - ETF份额: AKShare ak.fund_et_spot_em()
│   字段: 成交额占比、ETF净申购
│   频率: 日度
│   用途: 资金流向评分
│
└── fetch_industry_policy() → list
    数据源: 政府官网 RSS (feedparser)
    字段: 产业政策文件、补贴政策
    频率: 不定期
    用途: 政策支持评分
```

### 5.2 创建行业评分引擎 `app/services/industry_score_engine.py`

```
IndustryScoreEngine 类

├── SCORING_WEIGHTS: dict
│   ├── 库存周期位置: 30分
│   ├── 盈利趋势:     20分
│   ├── 需求增速:     20分
│   ├── 资金流向:     15分
│   └── 政策支持:     15分
│
├── identify_inventory_cycle(inventory_yoy, ppi_yoy) → str
│   库存降 + 价格升 → "被动去库" (景气即将上行, 30分)
│   库存升 + 价格升 → "主动补库" (景气高峰, 25分)
│   库存升 + 价格降 → "被动补库" (景气下行, 15分)
│   库存降 + 价格降 → "主动去库" (景气低谷, 10分)
│
├── score_profitability(roe, margin, trend) → dict
│   ROE连续两季回升且>历史50%分位 → 高分
│   毛利率连续回升 → 加分
│
├── score_demand(order_yoy, sales_yoy) → dict
│   同比>20%且持续 → 高分
│   同比0~20% → 中分
│   同比<0 → 低分
│
├── score_capital_flow(volume_pct, etf_flow) → dict
│   成交额占比提升 + ETF净申购 → 高分
│
├── score_policy(policy_count, policy_level) → dict
│   国家级规划 + 配套资金 → 高分
│   地方性政策 → 中分
│   无明显政策 → 低分
│
├── calculate_total(industry: str) → dict
│   总分 ≥ 70 → "超配"
│   总分 50~70 → "标配"
│   总分 < 50 → "低配"
│
└── evaluate_all() → list[dict]
    遍历所有行业 → 逐个评分 → 排序 → 入库
```

### 5.3 API 接口

```
GET  /api/calendar/industry/ranking                   # 行业景气排名(按评分排序)
GET  /api/calendar/industry/{name}/detail             # 单行业五维度详情
POST /api/calendar/industry/{name}/score              # 触发单行业评分
POST /api/calendar/industry/refresh                    # 全行业重新评分
GET  /api/calendar/industry/{name}/history             # 单行业评分历史
GET  /api/calendar/industry/inventory-cycle-map        # 库存周期象限图数据
```

---

## 二、前端任务

### 5.4 行业排名表格 `frontend/src/components/Calendar/IndustryRankingTable.vue`

```
el-table 列定义:
├── 排名
├── 行业名称
├── 景气评分 (数字 + 颜色条)
├── 配置建议标签 (超配=绿 / 标配=黄 / 低配=红)
├── 库存周期阶段
├── 核心催化剂
└── 操作 (查看详情按钮)

点击行 → 展开五维度指标
```

### 5.5 行业详情雷达图 `frontend/src/components/Calendar/IndustryRadar.vue`

```
ECharts Radar 图表
维度: 库存周期(30), 盈利趋势(20), 需求增速(20), 资金流向(15), 政策支持(15)
```

### 5.6 库存周期象限图

在 IndustryView.vue 中增加：
```
ECharts Scatter 散点图
├── X轴: PPI同比
├── Y轴: 库存同比
├── 四象限: 主动去库/被动去库/主动补库/被动补库
└── 各行业标注为散点
```

### 5.7 IndustryView.vue 页面

```
<template> 布局:
├── 顶部操作栏
│   ├── "刷新评分" 按钮
│   └── 评分时间
├── 行业景气排名概览 (前5名卡片)
├── IndustryRankingTable (完整排名表格)
├── 库存周期象限图
└── 行业详情弹窗 (点击某行业时)
    ├── IndustryRadar (五维度雷达图)
    ├── 各维度明细数据
    └── 核心催化剂
```

---

## 三、所需库

| 库 | 用途 | 是否已有 |
|---|------|---------|
| Tushare `industry_indicator` | 行业毛利率/ROE | ✅ 已有 |
| AKShare `stock_board_industry_name_em` | 行业成交额 | ✅ 已有 |
| AKShare `car_sales` | 汽车销量 | ✅ 已有 |
| AKShare `fund_et_spot_em` | ETF数据 | ✅ 已有 |
| feedparser | 政策RSS | ✅ 已有 |
| pandas | 分位数计算 | ✅ 已有 |

---

## 四、Phase 5 完成标准

- [ ] 全行业评分排名表格正确展示
- [ ] 库存周期象限图标注各行业位置
- [ ] 点击行业能查看五维度雷达图和明细
- [ ] 配置建议(超配/标配/低配)与评分对应
