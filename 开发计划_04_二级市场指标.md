# Phase 4 - 二级市场指标

> 目标：监控成交额、涨跌家数、涨停跌停、融资融券等市场微观指标，与宏观数据联动。

---

## 一、后端任务

### 4.1 创建采集器 `app/services/market_indicator_fetcher.py`

```
MarketIndicatorFetcher 类

├── fetch_advance_decline() → list
│   数据源: AKShare ak.stock_market_activity_lg()
│   字段: 上涨家数、下跌家数、平盘家数
│   频率: 日度
│
├── fetch_limit_stats() → list
│   数据源: AKShare ak.stock_zt_pool_em() (涨停)
│          ak.stock_zt_pool_dtgc_em() (跌停)
│   字段: 涨停数、跌停数
│   频率: 日度
│
├── fetch_turnover() → list
│   数据源: AKShare / Tushare
│   字段: 全市场换手率
│   频率: 日度
│
├── fetch_margin_trading() → list
│   数据源: AKShare ak.stock_margin_detail_szse()
│   字段: 融资余额、融券余额、融资买入额
│   频率: 日度
│
├── fetch_sector_ranking() → list
│   数据源: AKShare ak.stock_board_industry_name_em()
│   字段: 板块名称、涨跌幅、成交额
│   频率: 日度
│
└── fetch_volume() → list
    数据源: AKShare
    字段: 全市场成交额
    频率: 日度
```

### 4.2 背离检测 `app/services/divergence_detector.py`

```
DivergenceDetector 类
├── detect_macro_market_divergence(macro_score, market_data) → list
│   规则1: 宏观评分下降 + 成交额连续3天放量 → "虚假繁荣"预警
│   规则2: 宏观评分下降 + 涨停数增加 → "情绪与基本面背离"
│   规则3: 宏观评分上升 + 市场缩量下跌 → "机会信号"提示
│
└── detect_lie_opportunity(signals) → dict
    综合输出"谎言"/"机会"判断
```

### 4.3 API 接口

```
GET  /api/calendar/macro/market-indicators          # 二级市场指标概览
GET  /api/calendar/macro/market-indicators/history     # 历史数据
GET  /api/calendar/macro/divergence                    # 背离信号检测
GET  /api/calendar/macro/lie-opportunity               # 谎言/机会判断
POST /api/calendar/macro/market-indicators/fetch       # 手动采集
```

---

## 二、前端任务

### 4.4 二级市场指标组件

```
frontend/src/components/Calendar/
├── MarketIndicatorCards.vue
│   ├── 成交额 (对比前日)
│   ├── 涨跌家数 (红绿对比)
│   ├── 涨停/跌停数量
│   ├── 融资融券余额
│   └── 全市场换手率
│
├── AdvanceDeclineChart.vue
│   ECharts: 涨跌家数对比柱状图 (历史趋势)
│
├── LimitStatsChart.vue
│   ECharts: 涨停/跌停数量趋势图
│
├── VolumeChart.vue
│   ECharts: 成交额与指数叠加图
│
├── SectorHeatmap.vue
│   ECharts: 板块热度排名 (treemap)
│
└── DivergenceAlert.vue
    背离信号预警卡片 (红色=谎言, 绿色=机会)
```

### 4.5 MacroView.vue 追加

在宏观感知页面的 MacroView.vue 中增加"二级市场"折叠面板：

```
<el-collapse-item title="二级市场指标" name="market">
  ├── MarketIndicatorCards
  ├── AdvanceDeclineChart + LimitStatsChart (并排)
  ├── VolumeChart
  ├── SectorHeatmap
  └── DivergenceAlert (有背离时显示)
</el-collapse-item>
```

---

## 三、所需库

全部已有，无需新装。

---

## 四、Phase 4 完成标准

- [ ] 二级市场指标卡片展示当天数据
- [ ] 涨跌家数、成交额等有历史趋势图
- [ ] 宏观与市场背离时有预警提示
