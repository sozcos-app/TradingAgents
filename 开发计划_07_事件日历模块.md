# Phase 7 - 市场事件日历

> 目标：日历视图展示重要市场事件（财报、政策、解禁、地缘等），支持提前提醒。

---

## 一、后端任务

### 7.1 创建交易日历工具 `app/services/trade_calendar.py`

```
TradeCalendar 类

├── is_trade_day(date) → bool
│   判断是否为交易日
│   数据源: chinese_calendar 库 + AKShare交易日历
│
├── get_trade_days(start, end) → list[date]
│   获取时间段内的交易日列表
│
├── get_next_trade_day(date, n=1) → date
│   获取未来第N个交易日
│
├── generate_futures_expiry(year) → list[date]
│   计算期指交割日（每月第三个周五）
│
├── generate_institution_periods(year) → list[dict]
│   机构考核期（每季末最后两周）
│   返回: [{ period: "Q1考核期", start, end }]
│
└── generate_macro_publish_dates(year, month) → list[dict]
    宏观数据发布日（统计局每月固定日）
    CPI/PPI: 每月9~10日
    PMI: 每月31日/次月1日
    社融: 每月10~15日
    GDP: 每季后15日左右
```

### 7.2 创建事件采集器 `app/services/event_fetcher.py`

```
EventFetcher 类

├── fetch_earnings_calendar(start, end) → list[MarketEvent]
│   数据源: AKShare / Tushare 财报披露日程
│   事件类型: "财报"
│
├── fetch_unlock_calendar(start, end) → list[MarketEvent]
│   数据源: AKShare ak.stock_restricted_release_summary_em()
│   事件类型: "解禁"
│   过滤: 解禁市值 > 10亿 才录入
│
├── fetch_index_adjustment() → list[MarketEvent]
│   数据源: MSCI/中证指数官网 (手动录入为主)
│   事件类型: "指数调整"
│
├── fetch_policy_events() → list[MarketEvent]
│   数据源: 证监会/交易所 RSS (feedparser)
│   事件类型: "政策变更"
│
├── fetch_geopolitical_events() → list[MarketEvent]
│   数据源: 外交部/新华社 RSS (feedparser)
│   事件类型: "地缘政治"
│
├── fetch_dragon_head_events() → list[MarketEvent]
│   数据源: Tushare pro.disclosure() (重大合同、新品发布)
│   事件类型: "龙头事件"
│
└── fetch_macro_publish_schedule(year, month) → list[MarketEvent]
    数据源: 固定日历规则 (trade_calendar.generate_macro_publish_dates)
    事件类型: "宏观数据发布"
```

### 7.3 事件影响力评估 `app/services/event_impact_evaluator.py`

```
EventImpactEvaluator 类

├── IMPACT_RULES: dict
│   预定义各事件类型的默认影响方向和建议
│
│   "财报":
│   │   超预期 → 利多 → 提前3天潜伏
│   │   不及预期 → 利空 → 提前回避
│   "解禁":
│   │   解禁>20亿 → 利空(高) → 解禁前5天回避
│   "指数调整":
│   │   尾盘异动 → 中性 → 提前一天布局
│   "地缘冲突":
│   │   利多黄金/军工 → 轻仓参与
│   "宏观数据":
│   │   超预期 → 利多/利空相关板块
│   ...
│
├── evaluate(event: MarketEvent) → MarketEvent
│   根据规则填充 impact_direction, impact_strength, affected_sectors, action_suggestion
│
└── batch_evaluate(events: list) → list
    批量评估
```

### 7.4 API 接口

```
GET    /api/calendar/events                        # 事件列表(分页+筛选)
GET    /api/calendar/events/upcoming                # 未来N天事件
GET    /api/calendar/events/{id}                    # 事件详情
POST   /api/calendar/events                         # 手动创建事件
PUT    /api/calendar/events/{id}                    # 更新事件
DELETE /api/calendar/events/{id}                    # 删除事件
GET    /api/calendar/events/types                   # 事件类型字典
GET    /api/calendar/trade-days                     # 交易日历
POST   /api/calendar/events/fetch                   # 手动触发事件采集
```

---

## 二、前端任务

### 7.5 日历格子组件 `frontend/src/components/Calendar/EventCalendarGrid.vue`

```
使用 Element Plus el-calendar 或自定义月历网格
├── 月历/周历切换 (el-radio-group)
├── 每个日期格子:
│   ├── 日期数字
│   ├── 事件标记点 (颜色区分: 利多=绿, 利空=红, 中性=灰)
│   ├── 非交易日灰色背景
│   └── 休市日特殊标记
├── 点击日期 → 展开当日事件列表
└── 点击事件 → 打开详情弹窗

数据源: calendarApi.events.list()
```

### 7.6 事件详情弹窗 `frontend/src/components/Calendar/EventDetailDialog.vue`

```
el-dialog 弹窗
├── 事件标题
├── 事件类型标签
├── 事件日期
├── 影响方向 (利多=绿 / 利空=红 / 中性=灰)
├── 影响强度 (高/中/低)
├── 影响板块列表 (标签)
├── 行动建议 (文字描述)
├── 事件描述
└── 信息来源链接
```

### 7.7 事件编辑表单 `frontend/src/components/Calendar/EventFormDialog.vue`

```
el-dialog + el-form
├── 事件标题 (el-input)
├── 事件类型 (el-select: 财报/政策/解禁/地缘/指数调整/宏观数据/其他)
├── 事件日期 (el-date-picker)
├── 影响方向 (el-radio: 利多/利空/中性)
├── 影响强度 (el-radio: 高/中/低)
├── 影响板块 (el-select multiple)
├── 行动建议 (el-input textarea)
├── 事件描述 (el-input textarea)
└── 信息来源 (el-input)
```

### 7.8 EventCalendarView.vue 页面

```
<template> 布局:
├── 顶部操作栏
│   ├── 月份切换 (< 2026年6月 >)
│   ├── "添加事件" 按钮 → 打开 EventFormDialog
│   ├── "刷新" 按钮 → 触发事件采集
│   └── 筛选 (事件类型下拉 / 影响方向下拉)
├── EventCalendarGrid (日历格子)
├── EventDetailDialog (点击事件时弹出)
└── EventFormDialog (添加/编辑时弹出)
```

---

## 三、所需库

| 库 | 用途 | 是否已有 |
|---|------|---------|
| **chinese_calendar** | **A股休市/工作日判断** | **需新装** |
| feedparser | RSS 解析 | ✅ 已有 |
| AKShare | 财报日程/解禁 | ✅ 已有 |
| Tushare | 公告/披露 | ✅ 已有 |

安装命令：
```bash
pip install chinese_calendar
```

---

## 四、Phase 7 完成标准

- [ ] 月历视图正确显示，非交易日灰色标记
- [ ] 事件在日历格子上有颜色标记
- [ ] 点击事件能查看详情（影响方向、板块、建议）
- [ ] 支持手动添加/编辑/删除事件
- [ ] "即将到来"事件列表可查看
- [ ] 期指交割日、机构考核期等固定事件自动生成
