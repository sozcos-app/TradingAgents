# 交易日历模块 - 开发清单

> 基于 TradingAgents-CN 前后端分离架构（FastAPI + Vue 3 + MongoDB + Redis）进行二开。

---

## 一、整体技术栈总览

| 分类 | 技术/库 | 用途说明 |
|------|---------|----------|
| **后端框架** | FastAPI + Uvicorn | 项目已有，新增路由复用 |
| **数据库** | MongoDB (motor) | 项目已有，宏观数据、事件日历等结构化数据存储 |
| **缓存** | Redis | 项目已有，数据缓存、评分结果缓存 |
| **数据源-宏观数据** | AKShare | PMI、CPI、PPI、M2、社融、利率等（项目已集成） |
| **数据源-宏观数据** | Tushare | 宏观指标、行业指标、个股财务、解禁数据（项目已集成） |
| **数据源-新闻/公告** | feedparser | 解析 RSS 订阅（央行公告、证监会、交易所、外交部等） |
| **数据源-网页爬虫** | httpx + parsel | 项目已有，爬取国家统计局、巨潮资讯网、生意社等 |
| **数据源-反爬** | curl-cffi | 项目已有，模拟真实浏览器 TLS 指纹绕过反爬 |
| **数据源-个股财务** | Tushare `income`/`disclosure` | 主营业务构成、扣非净利润、财报披露 |
| **数据源-行业数据** | AKShare `stock_sector_spot` | 行业成交额、板块排名 |
| **数据源-行业数据** | Tushare `industry_indicator` | 行业毛利率、ROE |
| **数据源-解禁** | AKShare 解禁日历接口 | 限售股解禁日期、解禁市值 |
| **数据源-资金流向** | Tushare `moneyflow_ind_dc` | 行业资金流向 |
| **数据源-北向资金** | AKShare / Tushare | 北向资金净流入 |
| **数据源-汽车销量** | AKShare `car_sales` | 中汽协汽车销量数据 |
| **数据源-汇率利率** | AKShare | DR007、SHIBOR、人民币汇率 |
| **数据处理** | pandas | 项目已有，时间序列分析、环比同比计算、分位数计算 |
| **图表** | ECharts (vue-echarts) | 项目已有前端，日历视图、折线图、分位图、仪表盘 |
| **定时任务** | APScheduler | 项目已有，宏观数据定期采集、事件提醒 |
| **前端框架** | Vue 3 + TypeScript + Element Plus | 项目已有 |
| **前端状态管理** | Pinia | 项目已有 |
| **前端路由** | Vue Router 4 | 项目已有 |
| **AI 分析** | LangChain + LLM | 项目已有，用于"谎言/机会识别"等深度分析 |
| **日期处理** | dayjs | 项目已有前端，交易日历计算 |
| **交易日历工具** | `chinese_calendar` 或自建规则 | A股休市日、节假日判断 |
| **规则引擎** | 自建（Python dict + callable） | 宏观评分规则、行业景气评分、个股健康度评分 |

---

## 二、模块开发分步清单

### 第一步：基础框架搭建（前后端路由、模型定义）

> 目标：在现有项目中创建交易日历模块的骨架，能跑通空页面和空接口。

#### 后端

- [ ] **1.1 创建模型文件 `app/models/calendar.py`**
  - `MacroIndicator` - 宏观指标文档（PMI、社融、CPI、PPI、M2、DR007 等）
  - `IndustryProsperity` - 行业景气度文档
  - `StockHealth` - 个股健康度文档
  - `MarketEvent` - 市场事件日历文档
  - `MacroScore` - 宏观评分记录
  - `IndustryScore` - 行业评分记录
  - `StockHealthScore` - 个股评分记录

- [ ] **1.2 创建 Schema 文件 `app/schemas/calendar.py`**
  - 各模型的 Pydantic 请求/响应 Schema
  - 评分结果 Schema（分数、等级、结论）
  - 筛选和分页参数 Schema

- [ ] **1.3 创建服务文件 `app/services/calendar_service.py`**
  - `MacroDataService` - 宏观数据采集与分析
  - `IndustryProsperityService` - 行业景气分析
  - `StockHealthService` - 个股体检
  - `MarketEventService` - 市场事件管理
  - `ScoreEngineService` - 评分引擎（规则计算）

- [ ] **1.4 创建路由文件 `app/routers/calendar.py`**
  - `/api/calendar/macro` - 宏观感知相关接口
  - `/api/calendar/industry` - 行业景气相关接口
  - `/api/calendar/stock-health` - 个股体检相关接口
  - `/api/calendar/events` - 市场事件日历接口

- [ ] **1.5 在 `app/main.py` 中注册路由**
  ```python
  app.include_router(calendar.router, prefix="/api/calendar", tags=["calendar"])
  ```

- [ ] **1.6 创建采集 Worker `app/worker/macro_worker.py`**
  - 定时采集宏观数据的任务调度
  - 数据入库与去重逻辑

#### 前端

- [ ] **1.7 创建 API 模块 `frontend/src/api/calendar.ts`**
  - `calendarApi` 对象，封装所有交易日历相关接口调用

- [ ] **1.8 创建类型定义 `frontend/src/types/calendar.ts`**
  - 各实体的 TypeScript 接口定义

- [ ] **1.9 创建 Store `frontend/src/stores/calendar.ts`**
  - Pinia store 管理交易日历状态

- [ ] **1.10 创建页面组件**
  - `frontend/src/views/Calendar/index.vue` - 交易日历主页面（Tab 切换四个模块）
  - `frontend/src/views/Calendar/MacroView.vue` - 宏观感知页面
  - `frontend/src/views/Calendar/IndustryView.vue` - 行业景气页面
  - `frontend/src/views/Calendar/StockHealthView.vue` - 个股体检页面
  - `frontend/src/views/Calendar/EventCalendarView.vue` - 市场事件日历页面

- [ ] **1.11 在 `frontend/src/router/index.ts` 中注册路由**
  ```typescript
  {
    path: '/calendar',
    name: 'Calendar',
    component: () => import('@/layouts/BasicLayout.vue'),
    redirect: '/calendar/macro',
    children: [
      { path: 'macro', name: 'MacroView', component: () => import('@/views/Calendar/MacroView.vue') },
      { path: 'industry', name: 'IndustryView', component: () => import('@/views/Calendar/IndustryView.vue') },
      { path: 'stock-health', name: 'StockHealthView', component: () => import('@/views/Calendar/StockHealthView.vue') },
      { path: 'events', name: 'EventCalendarView', component: () => import('@/views/Calendar/EventCalendarView.vue') },
    ]
  }
  ```

- [ ] **1.12 在侧边栏导航菜单中添加"交易日历"入口**

---

### 第二步：宏观数据采集模块（REQ-001, REQ-002, REQ-009, REQ-014）

> 目标：能自动/手动获取 PMI、社融、CPI、PPI、M2、DR007、利率、汇率、北向资金、央行公开市场操作等数据并存入 MongoDB。

#### 数据源与所需接口

| 指标 | 数据源 | AKShare 接口 / Tushare 接口 | 采集频率 |
|------|--------|----------------------------|----------|
| PMI 制造业/非制造业 | 国家统计局 / AKShare | `ak.macro_china_pmi()` | 月度 |
| 社融存量/新增 | 央行 / Tushare | `pro.shm()`（需积分） | 月度 |
| CPI 同比/环比 | 国家统计局 / AKShare | `ak.macro_china_cpi()` | 月度 |
| PPI 同比/环比 | 国家统计局 / AKShare | `ak.macro_china_ppi()` | 月度 |
| M2 同比 | 央行 / AKShare | `ak.macro_china_money_supply()` | 月度 |
| DR007 | CFETS / AKShare | `ak.rate_interbank(money_type="DR007")` | 日度 |
| 7天逆回购利率 | 央行公告 / 爬虫 | 爬取央行官网公开市场操作公告 | 不定期 |
| MLF 中标利率 | 央行公告 / 爬虫 | 爬取央行官网 | 不定期 |
| 人民币汇率 | AKShare | `ak.currency_boc()` | 日度 |
| 北向资金净流入 | AKShare / Tushare | `ak.stock_hsgt_north_net_flow_in_em()` | 日度 |
| 央行公开市场操作 | 央行官网 / RSS | feedparser 解析 RSS | 每日检查 |

#### 后端任务

- [ ] **2.1 创建数据采集器 `app/services/macro_data_fetcher.py`**
  - `MacroDataFetcher` 类，封装所有宏观数据源的调用
  - 每个指标一个方法，统一返回 `List[MacroIndicator]`
  - 错误处理与重试机制
  - 数据去重（按指标名+日期判重）

- [ ] **2.2 央行公开市场操作爬虫**
  - 使用 feedparser 解析中国人民银行公开市场操作公告 RSS
  - 解析逆回购/MLF 操作量、中标利率、净投放/回笼
  - 存入 `MacroIndicator` 集合，type 为 `open_market_operation`

- [ ] **2.3 数据手动录入接口**
  - `POST /api/calendar/macro/manual` - 支持手动录入/修正数据
  - 用于数据源缺失时的补充

- [ ] **2.4 数据查询接口**
  - `GET /api/calendar/macro/indicators` - 查询所有宏观指标
  - `GET /api/calendar/macro/indicators/{name}` - 查询单个指标的历史数据
  - `GET /api/calendar/macro/indicators/{name}/latest` - 获取最新值

- [ ] **2.5 定时采集任务**
  - 在 APScheduler 中注册宏观数据采集任务
  - 日度数据：每个交易日收盘后采集
  - 月度数据：每月初采集上月数据

#### 前端任务

- [ ] **2.6 宏观数据展示页面**
  - 指标卡片：显示各指标最新值及环比变化
  - 历史趋势图：ECharts 折线图展示指标历史走势
  - 数据来源标注

---

### 第三步：宏观感知评分模块（REQ-003, REQ-004, REQ-008）

> 目标：基于采集的宏观数据，自动计算宏观环境评分（0~100分），输出"可积极参与/谨慎试仓/防守观望"结论。

#### 后端任务

- [ ] **3.1 创建评分引擎 `app/services/score_engine.py`**
  - `MacroScoreEngine` 类
  - 各指标评分规则实现（参照需求清单中的判定标准）
  - 加权计算总分
  - 输出评分 + 等级 + 各指标明细

- [ ] **3.2 评分规则实现**
  ```
  PMI: >50 扩张(好), <50 收缩(坏)           → 权重 20%
  社融同比: >10% 宽信用(好), <8% 紧信用(坏)  → 权重 20%
  CPI: 2%~3% 温和(好), >3% 过热/<0% 通缩     → 权重 10%
  PPI: 回升且正值(好), 持续负值(坏)          → 权重 10%
  M2同比: 高于GDP增速(好), 低于或骤降(坏)    → 权重 10%
  DR007-政策利率差: 负值大(宽松好), 正值(紧) → 权重 15%
  北向资金: 连续5日均值>30亿(好), 流出(坏)   → 权重 15%
  ```

- [ ] **3.3 综合判定规则**
  - `>70分` → "可积极参与"
  - `40~70分` → "谨慎试仓"
  - `<40分` → "防守观望"
  - 异常波动自动预警（指标变化超过阈值）

- [ ] **3.4 评分接口**
  - `POST /api/calendar/macro/score` - 触发宏观评分计算
  - `GET /api/calendar/macro/score/latest` - 获取最新评分
  - `GET /api/calendar/macro/score/history` - 评分历史趋势

#### 前端任务

- [ ] **3.5 宏观评分仪表盘**
  - ECharts 仪表盘展示总分（0~100）
  - 等级标签显示（可积极参与/谨慎试仓/防守观望）
  - 各指标贡献度雷达图
  - 历史评分折线图

- [ ] **3.6 异常预警提示**
  - 指标异常波动时红色标记
  - Toast 通知提醒

---

### 第四步：二级市场指标模块（REQ-005, REQ-006, REQ-007）

> 目标：监控成交额、涨跌家数、涨停跌停、融资融券等市场微观指标，并与宏观数据联动分析。

#### 数据源与所需接口

| 指标 | 数据源 | 接口 |
|------|--------|------|
| 涨跌家数 | AKShare | `ak.stock_market_activity_lg(symbol="上证指数")` |
| 涨停/跌停 | AKShare / Tushare | `ak.stock_zt_pool_em()` / `ak.stock_zt_pool_dtgc_em()` |
| 成交额 | AKShare | `ak.stock_market_activity_lg()` |
| 融资融券 | AKShare | `ak.stock_margin_detail_szse()` / `ak.stock_margin_sse()` |
| 板块排名 | AKShare | `ak.stock_board_industry_name_em()` |
| 换手率 | Tushare | `pro.daily(ts_code=...)` |

#### 后端任务

- [ ] **4.1 创建 `app/services/market_indicator_fetcher.py`**
  - 二级市场指标数据采集器
  - 交易日收盘后自动采集

- [ ] **4.2 数据接口**
  - `GET /api/calendar/macro/market-indicators` - 二级市场指标概览
  - `GET /api/calendar/macro/market-indicators/history` - 历史数据

- [ ] **4.3 宏观与二级指标联动分析**
  - "背离信号"检测（如：宏观评分下降但市场放量上涨）
  - "谎言/机会识别"规则引擎（REQ-007）

#### 前端任务

- [ ] **4.4 二级市场指标面板**
  - 涨跌家数对比图
  - 涨停跌停数量趋势
  - 成交额与指数走势叠加图
  - 融资融券余额变化图
  - 板块热度排名

---

### 第五步：行业景气模块（REQ-010, REQ-015~019）

> 目标：计算行业景气评分（0~100分），输出行业景气排名和配置建议（超配/标配/低配）。

#### 数据源与所需接口

| 维度 | 指标 | 数据源 | 接口 |
|------|------|--------|------|
| 库存周期 | 产成品库存同比 | 国家统计局 | `ak.macro_china_inventory()` |
| 库存周期 | PPI 分行业 | AKShare | `ak.macro_china_ppi()` |
| 盈利 | 行业毛利率/ROE | Tushare | `pro.industry_indicator()` |
| 需求 | 汽车销量 | AKShare | `ak.car_sales_2024()` |
| 需求 | 行业出货量 | Tushare | `pro.disclosure()` |
| 政策 | 产业政策 | 政府官网 RSS | feedparser |
| 资金 | 行业成交额占比 | AKShare | `ak.stock_board_industry_name_em()` |
| 资金 | ETF 份额变化 | AKShare / 爬虫 | `ak.fund_et_spot_em()` |

#### 后端任务

- [ ] **5.1 创建 `app/services/industry_fetcher.py`**
  - 行业景气数据采集器
  - 库存、盈利、需求、资金、政策五维度数据采集

- [ ] **5.2 创建 `app/services/industry_score_engine.py`**
  - `IndustryScoreEngine` 类
  - 评分规则：库存周期位置 30 分，盈利趋势 20 分，需求增速 20 分，资金流向 15 分，政策 15 分
  - 库存周期判定逻辑（主动去库/被动去库/主动补库/被动补库）

- [ ] **5.3 数据接口**
  - `GET /api/calendar/industry/list` - 行业景气排名列表
  - `GET /api/calendar/industry/{industry_name}/detail` - 单行业详情
  - `GET /api/calendar/industry/{industry_name}/score` - 单行业评分详情
  - `POST /api/calendar/industry/refresh` - 手动触发评分计算

#### 前端任务

- [ ] **5.4 行业景气排名页面**
  - 表格展示行业排名（评分、等级、配置建议）
  - 点击展开查看五维度详细指标

- [ ] **5.5 单行业详情页面**
  - 行业景气评分仪表盘
  - 库存周期象限图（库存同比 vs PPI 同比）
  - 五维度雷达图
  - 核心催化剂标注

---

### 第六步：个股体检模块（REQ-011, REQ-012, REQ-020~023）

> 目标：计算个股健康度评分（0~100分），输出风险等级和"可关注/谨慎/回避"结论。

#### 数据源与所需接口

| 指标 | 数据源 | 接口 |
|------|--------|------|
| 主营业务收入构成 | Tushare / 巨潮资讯网 | `pro.income()` / 爬虫解析年报 |
| 扣非净利润/归属净利润 | Tushare | `pro.income()` |
| 个股毛利率 | Tushare | `pro.income()` |
| 行业毛利率均值 | Tushare | `pro.industry_indicator()` |
| 信披记录（处罚/问询） | 交易所 / 证监会 | 爬虫（巨潮资讯网问询函、处罚公告） |
| 供应链公告 | 巨潮资讯网 | httpx + parsel 爬虫（临时公告、年报） |
| 大股东增减持 | Tushare / AKShare | `pro.disclosure()` / `ak.stock_fhps_em()` |
| 限售股解禁 | AKShare | `ak.stock_restricted_release_summary_em()` |

#### 后端任务

- [ ] **6.1 创建 `app/services/stock_health_fetcher.py`**
  - 个股体检数据采集器
  - 财务数据、信披数据、公告数据采集

- [ ] **6.2 巨潮资讯网爬虫 `app/services/crawl_cninfo.py`**
  - 解析年报"营业收入构成"章节
  - 爬取问询函、处罚公告、重大合同公告
  - 反爬处理（curl-cffi + 请求间隔）

- [ ] **6.3 创建 `app/services/stock_health_score_engine.py`**
  - `StockHealthScoreEngine` 类
  - 评分规则：主营业务匹配度 30 分，利润含金量 25 分，毛利率合理 20 分，信披记录 15 分，供应链验证 10 分
  - 排雷规则触发检测

- [ ] **6.4 数据接口**
  - `POST /api/calendar/stock-health/check` - 对单只股票进行体检
  - `GET /api/calendar/stock-health/{ts_code}/report` - 获取体检报告
  - `GET /api/calendar/stock-health/{ts_code}/alerts` - 获取预警信息

#### 前端任务

- [ ] **6.5 个股体检页面**
  - 输入股票代码/名称，触发体检
  - 健康度评分仪表盘
  - 五维度明细卡片（含好/坏判定及数据来源）
  - 风险等级标签（低/中/高）

- [ ] **6.6 排雷预警列表**
  - 红色标记触发的排雷规则
  - 点击查看详情和原始数据来源

---

### 第七步：市场事件日历模块（REQ-013, REQ-024~030）

> 目标：日历视图展示重要市场事件，支持提前提醒。

#### 数据源与所需接口

| 事件类型 | 数据源 | 采集方式 |
|----------|--------|----------|
| 财报披露截止日 | AKShare / Tushare | `ak.stock_info_a_code_name()` 获取财报披露日程 |
| A股休市/节假日 | 自建规则库 | `chinese_calendar` 库 + 自定义交易日历 |
| 期指交割日 | 固定规则（每月第三个周五） | Python `calendar` 计算 |
| 机构考核期 | 固定规则（季末/年末） | Python `calendar` 计算 |
| 限售股解禁 | AKShare | `ak.stock_restricted_release_summary_em()` |
| 央行公开市场操作 | 央行官网 RSS | feedparser |
| 地缘政治/高层会晤 | 外交部/新华社 RSS | feedparser |
| 龙头企业重大事件 | Tushare / 科技媒体 RSS | `pro.disclosure()` + feedparser |
| 指数调整（MSCI/富时/中证） | 官网 / 手动录入 | 手动为主 + RSS |
| 交易制度变更 | 证监会/交易所 | RSS + 手动录入 |
| 宏观数据发布日历 | 经济数据日历 | 固定发布日（统计局每月 9/10/15 日等） |

#### 后端任务

- [ ] **7.1 创建 `app/services/event_fetcher.py`**
  - `EventFetcher` 类，聚合所有事件源
  - RSS 订阅管理（外交部、央行、证监会、交易所）
  - 固定日历规则生成（期指交割日、机构考核期、宏观数据发布日）

- [ ] **7.2 交易日历工具 `app/services/trade_calendar.py`**
  - A股交易日判断
  - 节假日/休市日生成
  - 期指交割日计算（每月第三个周五）
  - 机构考核期标记（每季末最后两周）

- [ ] **7.3 事件影响力评估**
  - 事件类型 → 影响方向（利多/利空/中性）
  - 影响强度（高/中/低）
  - 关联板块标注
  - 行动建议生成

- [ ] **7.4 数据接口**
  - `GET /api/calendar/events` - 按时间范围查询事件
  - `GET /api/calendar/events/upcoming` - 即将到来事件（7天内）
  - `POST /api/calendar/events/manual` - 手动录入事件
  - `PUT /api/calendar/events/{id}` - 编辑事件
  - `DELETE /api/calendar/events/{id}` - 删除事件
  - `GET /api/calendar/events/types` - 获取事件类型列表
  - `GET /api/calendar/trade-days` - 获取交易日历

#### 前端任务

- [ ] **7.5 事件日历视图**
  - 月历/周历切换视图
  - 事件标记在日历格子上（颜色区分利多/利空）
  - 点击日期展开事件详情

- [ ] **7.6 事件详情弹窗**
  - 事件名称、类型、时间
  - 影响方向与强度
  - 关联板块/个股
  - 行动建议

- [ ] **7.7 事件筛选与搜索**
  - 按类型筛选（财报、政策、解禁、地缘等）
  - 按影响方向筛选
  - 关键词搜索

- [ ] **7.8 事件提醒功能**
  - 提前 1~7 天提醒（可配置）
  - 通知中心展示

---

### 第八步：AI 分析集成（REQ-007 谎言/机会识别）

> 目标：利用项目已有的 LLM 能力，基于指标组合输出"谎言/机会"提示。

#### 后端任务

- [ ] **8.1 创建 `app/services/market_insight_service.py`**
  - `MarketInsightService` 类
  - 将指标数据 + 评分结果组装为 Prompt
  - 调用 LLM 生成"谎言/机会"分析报告
  - 支持流式输出（SSE）

- [ ] **8.2 Prompt 模板设计**
  - 宏观数据偏离分析 Prompt
  - 市场与宏观背离分析 Prompt
  - 行业景气异常分析 Prompt
  - 个股风险识别 Prompt

- [ ] **8.3 数据接口**
  - `POST /api/calendar/macro/insight` - 触发宏观 AI 分析
  - `POST /api/calendar/industry/{name}/insight` - 触发行业 AI 分析
  - `POST /api/calendar/stock-health/{ts_code}/insight` - 触发个股 AI 分析

#### 前端任务

- [ ] **8.4 AI 分析结果展示**
  - 分析报告卡片
  - Markdown 渲染（项目已有 `marked` 库）
  - 谎言/机会标签高亮

---

### 第九步：定时任务与自动化

> 目标：数据自动采集、评分自动计算、事件自动提醒。

#### 后端任务

- [ ] **9.1 注册 APScheduler 任务**
  - 宏观数据日度采集（每个交易日 16:00）
  - 宏观数据月度采集（每月 10 日）
  - 宏观评分自动计算（数据更新后触发）
  - 行业景气评分（每周一计算）
  - 事件日历更新（每日检查 RSS）
  - 事件提醒推送（提前 N 天）

- [ ] **9.2 任务管理接口**
  - `GET /api/calendar/tasks` - 查看所有定时任务状态
  - `POST /api/calendar/tasks/{task_id}/trigger` - 手动触发任务
  - `PUT /api/calendar/tasks/{task_id}/toggle` - 启用/禁用任务

#### 前端任务

- [ ] **9.3 定时任务管理面板**
  - 在设置页面添加交易日历任务管理
  - 任务状态显示（最近执行时间、下次执行时间、成功/失败）
  - 手动触发按钮

---

### 第十步：测试与优化

- [ ] **10.1 后端单元测试**
  - 评分引擎测试（验证边界值）
  - 数据采集器测试（Mock 数据源）
  - API 接口测试

- [ ] **10.2 前端联调**
  - 各页面接口联调
  - 图表渲染验证
  - 响应式布局检查

- [ ] **10.3 数据完整性验证**
  - 验证各数据源接口可用性
  - 处理数据缺失场景
  - 异常数据容错处理

---

## 三、新增第三方依赖汇总

### Python 后端

```
# 爬虫与数据采集（大部分项目已有）
feedparser>=6.0.11        # RSS 解析（央行、证监会、外交部公告）
httpx>=0.24.0             # HTTP 客户端（项目已有）
parsel>=1.10.0            # HTML 解析（项目已有）
curl-cffi>=0.6.0          # 反爬绕过（项目已有）

# 交易日历（新增）
chinese_calendar>=1.9.0   # 中国法定节假日/工作日判断
# 或用项目自建的交易日历数据

# 数据处理（项目已有）
pandas>=2.3.0
```

> 注：AKShare、Tushare、feedparser、httpx、parsel、curl-cffi、pandas 等核心库项目 `pyproject.toml` 中已包含，**无需额外安装**。唯一可能需要新增的是 `chinese_calendar`。

### 前端

```
# 无需额外安装，项目已有：
# vue-echarts - 图表
# dayjs - 日期处理
# marked - Markdown 渲染
# element-plus - UI 组件
```

---

## 四、MongoDB 新增集合

| 集合名 | 用途 | 索引建议 |
|--------|------|----------|
| `macro_indicators` | 宏观指标数据（PMI、CPI 等） | `{indicator_name: 1, date: -1}` 唯一索引 |
| `market_indicators` | 二级市场指标（涨跌家数、成交额等） | `{date: -1}` |
| `industry_prosperity` | 行业景气数据 | `{industry_name: 1, date: -1}` |
| `industry_scores` | 行业景气评分记录 | `{date: -1, score: -1}` |
| `stock_health_checks` | 个股体检记录 | `{ts_code: 1, check_date: -1}` |
| `market_events` | 市场事件日历 | `{event_date: -1, type: 1}` |
| `trade_calendar` | A股交易日历 | `{date: 1}` 唯一索引 |
| `open_market_operations` | 央行公开市场操作 | `{date: -1}` |
| `macro_scores` | 宏观评分历史 | `{date: -1}` |
| `ai_insights` | AI 分析报告 | `{type: 1, created_at: -1}` |

---

## 五、开发优先级路线

```
Phase 1（核心骨架）  → 第一步：基础框架搭建
Phase 2（数据基础）  → 第二步：宏观数据采集 + 第七步部分：交易日历工具
Phase 3（核心分析）  → 第三步：宏观评分 + 第五步：行业景气评分
Phase 4（个股分析）  → 第六步：个股体检
Phase 5（事件驱动）  → 第七步：市场事件日历完整实现
Phase 6（智能增强）  → 第八步：AI 分析集成
Phase 7（自动化）    → 第九步：定时任务
Phase 8（收尾）      → 第四步（二级市场指标） + 第十步（测试优化）
```

建议按 Phase 1 → 2 → 3 顺序推进，先跑通"宏观感知"完整链路（采集→评分→展示），再扩展其他模块。
