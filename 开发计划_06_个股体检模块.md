# Phase 6 - 个股体检模块

> 目标：计算个股健康度评分(0~100)，输出风险等级和"可关注/谨慎/回避"结论。

---

## 一、后端任务

### 6.1 创建个股数据采集器 `app/services/stock_health_fetcher.py`

```
StockHealthFetcher 类

├── fetch_main_business(ts_code) → dict
│   数据源: Tushare pro.income() (主营业务收入构成)
│   字段: 主营收入、总营收、分业务占比
│   用途: 主营业务匹配度评分
│
├── fetch_profit_quality(ts_code) → dict
│   数据源: Tushare pro.income() (扣非净利润/归属净利润)
│   字段: 扣非净利润、归属净利润、比值
│   用途: 利润含金量评分
│
├── fetch_gross_margin(ts_code) → dict
│   数据源: Tushare pro.income() + pro.industry_indicator()
│   字段: 个股毛利率、行业均值
│   用途: 毛利率偏离度评分
│
├── fetch_disclosure_risk(ts_code) → dict
│   数据源: 巨潮资讯网爬虫 (问询函、处罚公告)
│   字段: 是否有处罚、问询次数、立案调查
│   用途: 信披评分
│
├── fetch_supply_chain(ts_code) → dict
│   数据源: 巨潮资讯网年报爬虫 (供应链披露)
│   字段: 是否进入龙头供应链、合同公告
│   用途: 供应链验证评分
│
└── fetch_unlock_pressure(ts_code) → dict
    数据源: AKShare ak.stock_restricted_release_summary_em()
    字段: 未来30天解禁市值、流通市值、占比
    用途: 解禁压力扣分
```

### 6.2 巨潮资讯网爬虫 `app/services/crawl_cninfo.py`

```
CninfoCrawler 类

├── search_disclosures(ts_code, type) → list[dict]
│   爬取巨潮资讯网公告搜索接口
│   type: "问询函" / "处罚" / "重大合同" / "减持" / "解禁"
│
├── parse_annual_report_business(ts_code) → dict
│   解析年报"营业收入构成"章节
│   返回: { 主营收入: float, 总营收: float, 占比: float, 业务列表: [...] }
│
├── parse_annual_report_supply_chain(ts_code) → dict
│   解析年报"主要客户/供应商"章节
│   返回: { 前五大客户, 前五大供应商, 供应链关系 }
│
├── fetch_inquiry_letters(ts_code, years=3) → list[dict]
│   获取近3年问询函列表及内容
│
└── fetch_penalties(ts_code, years=3) → list[dict]
    获取近3年处罚/立案调查记录
```

**反爬策略**：
- 使用 `curl-cffi` 模拟浏览器 TLS 指纹
- 请求间隔 2~5 秒随机
- User-Agent 轮换
- 失败重试 3 次

### 6.3 创建个股评分引擎 `app/services/stock_health_score_engine.py`

```
StockHealthScoreEngine 类

├── SCORING_WEIGHTS: dict
│   ├── 主营业务匹配度: 30分
│   ├── 利润含金量:     25分
│   ├── 毛利率合理性:   20分
│   ├── 信披记录:       15分
│   └── 供应链验证:     10分
│
├── score_main_business(ratio) → dict
│   ≥80% → 30分
│   60~80% → 20分
│   <60% → 5分 (降分且需合理解释)
│
├── score_profit_quality(ratio, trend) → dict
│   ≥0.9且持续 → 25分
│   0.7~0.9 → 15分
│   <0.7 → 5分 (依赖非经常性损益)
│
├── score_gross_margin(stock_margin, industry_avg) → dict
│   偏差<15% → 20分 (正常)
│   偏差15~20% → 10分 (需确认有技术壁垒)
│   偏差>20%且无合理解释 → 0分 (疑似造假)
│
├── score_disclosure(penalties, inquiries) → dict
│   无处罚无问询 → 15分
│   有问询无处罚 → 10分
│   有处罚 → 3分
│   有立案调查 → 0分
│
├── score_supply_chain(chain_data) → dict
│   进入龙头供应链且有合同 → 10分
│   进入供应链但无具体合同 → 5分
│   仅口头宣称"合作意向" → 0分
│
├── apply_deductions(base_score, alerts) → dict
│   解禁压力 >20% → 扣10分
│   大股东减持 >1% → 扣15分
│
├── calculate_total(ts_code) → StockHealthCheck
│   逐项评分 → 汇总 → 判断结论
│   ≥80分 → "可关注"
│   60~79分 → "谨慎"
│   <60分 → "回避"
│
└── detect_alerts(data) → list[dict]
    返回触发的排雷规则列表
    [{ rule: str, severity: str, message: str }]
```

### 6.4 API 接口

```
POST /api/calendar/stock-health/check
├── 参数: { ts_code: str }
├── 调用: 采集数据 → 评分 → 入库
└── 返回: StockHealthCheck 完整报告

GET  /api/calendar/stock-health/{ts_code}
├── 返回: 最近一次体检报告

GET  /api/calendar/stock-health/{ts_code}/alerts
├── 返回: 预警项列表

GET  /api/calendar/stock-health/history
├── 参数: ts_code, page, page_size
├── 返回: 历史体检记录
```

---

## 二、前端任务

### 6.5 个股体检主页面 `StockHealthView.vue`

```
<template> 布局:
├── 搜索栏
│   ├── 股票代码/名称搜索框 (el-autocomplete)
│   └── "开始体检" 按钮
├── 体检结果区域 (体检后显示)
│   ├── 顶部概览卡
│   │   ├── 股票名称 + 代码
│   │   ├── 健康度评分 (大号数字 + 颜色)
│   │   ├── 风险等级标签 (低=绿 / 中=黄 / 高=红)
│   │   └── 结论标签 (可关注/谨慎/回避)
│   ├── 五维度评分条
│   │   ├── 主营业务匹配度 [=========---] 25/30
│   │   ├── 利润含金量     [=========---] 22/25
│   │   ├── 毛利率合理性   [========----] 16/20
│   │   ├── 信披记录       [===========]  15/15
│   │   └── 供应链验证     [====-------]   4/10
│   ├── 预警列表 (红色标记)
│   └── 历史体检趋势 (折线图)
```

### 6.6 预警组件 `frontend/src/components/Calendar/StockHealthReport.vue`

```
排雷预警列表 (el-timeline)
每个预警项:
├── 严重程度图标 (🔴 高 / 🟡 中)
├── 规则名称 (如 "毛利率异常偏离")
├── 详细说明 (如 "毛利率62%，行业均值35%，偏差77%")
├── 数据来源链接
└── 建议操作 (如 "需确认是否有技术壁垒")
```

---

## 三、所需库

| 库 | 用途 | 是否已有 |
|---|------|---------|
| Tushare `income` | 财务数据 | ✅ 已有 |
| Tushare `industry_indicator` | 行业均值 | ✅ 已有 |
| AKShare `stock_restricted_release_summary_em` | 解禁数据 | ✅ 已有 |
| httpx + curl-cffi | 巨潮爬虫 | ✅ 已有 |
| parsel | HTML 解析 | ✅ 已有 |

---

## 四、Phase 6 完成标准

- [ ] 输入股票代码能触发体检并返回评分
- [ ] 五维度评分条正确展示各分项得分
- [ ] 触发排雷规则的指标有红色预警
- [ ] 历史体检趋势可查看
