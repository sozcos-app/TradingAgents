# Phase 3 - 宏观评分引擎

> 目标：基于采集的宏观数据，自动计算宏观环境评分(0~100)，输出"可积极参与/谨慎试仓/防守观望"结论。

---

## 一、后端任务

### 3.1 创建评分引擎 `app/services/macro_score_engine.py`

```
MacroScoreEngine 类

├── INDICATOR_WEIGHTS: dict     # 各指标权重
│   ├── PMI:              0.20
│   ├── 社融同比:          0.20
│   ├── CPI:              0.10
│   ├── PPI:              0.10
│   ├── M2同比:            0.10
│   ├── DR007利差:         0.15
│   └── 北向资金:          0.15
│
├── score_pmi(value: float) → dict
│   >51  → 100分
│   50~51 → 70分
│   49~50 → 40分
│   <49  → 10分
│   返回: { score, weight, direction, detail }
│
├── score_social_financing(yoy: float) → dict
│   >12%  → 100分
│   10~12% → 80分
│   8~10%  → 50分
│   <8%   → 10分
│
├── score_cpi(value: float) → dict
│   2~3%  → 100分 (温和通胀)
│   1~2% or 3~4% → 60分
│   <0% or >4% → 10分 (通缩/过热)
│
├── score_ppi(value: float) → dict
│   正值且回升 → 80分
│   正值且下降 → 50分
│   负值且回升 → 40分
│   负值且持续下降 → 10分
│
├── score_m2(m2_yoy: float, gdp_growth: float) → dict
│   M2同比 > GDP增速+2% → 100分 (宽松)
│   M2同比 > GDP增速 → 70分
│   M2同比 < GDP增速 → 30分
│   M2骤降(环比降>2%) → 10分
│
├── score_dr007_spread(dr007: float, policy_rate: float) → dict
│   利差 < -0.3% → 100分 (非常宽松)
│   利差 -0.3%~0% → 80分
│   利差 0%~0.3% → 50分
│   利差 > 0.3% → 10分 (偏紧)
│
├── score_northbound(flow_5day_avg: float) → dict
│   >50亿 → 100分
│   30~50亿 → 80分
│   0~30亿 → 50分
│   流出 → 10分
│
├── calculate_total(indicator_scores: dict) → dict
│   加权求和 → total_score
│   >70分 → "可积极参与"
│   40~70分 → "谨慎试仓"
│   <40分 → "防守观望"
│
└── evaluate() → MacroScore
    从 DB 读取最新指标数据 → 逐项评分 → 加权汇总 → 入库
```

### 3.2 在 MacroDataService 中实现评分调用

```python
async def calculate_score(self) -> MacroScore:
    """计算宏观评分"""
    engine = MacroScoreEngine()
    result = engine.evaluate()
    # 存入 macro_scores 集合
    return result
```

### 3.3 异常预警检测

在 `macro_score_engine.py` 中增加：

```python
def detect_anomalies(indicators: dict) -> list[dict]:
    """检测指标异常波动"""
    alerts = []
    # 1. 单指标环比变化超过阈值（如 >5%）
    # 2. PMI 连续3月下降
    # 3. 社融同比骤降超 3%
    # 4. 北向资金连续5日净流出
    return alerts  # [{ indicator, alert_type, message, severity }]
```

### 3.4 API 接口

```
POST /api/calendar/macro/score
├── 调用 macro_service.calculate_score()
└── 返回: { total_score, level, details, alerts }

GET /api/calendar/macro/score/latest
├── 从 macro_scores 集合取最新一条
└── 返回: { total_score, level, details, date }

GET /api/calendar/macro/score/history
├── 参数: start_date, end_date
└── 返回: 评分历史列表
```

---

## 二、前端任务

### 3.5 评分仪表盘 `frontend/src/components/Calendar/MacroScoreGauge.vue`

```
ECharts Gauge 图表
├── 刻度 0~100
├── 颜色区间: <40 红色, 40~70 橙色, >70 绿色
├── 中心显示总分和等级文字
└── 底部显示评分日期

数据源: calendarApi.macro.getLatestScore()
```

### 3.6 指标贡献度雷达图

在 MacroView.vue 中增加 ECharts Radar 图表：
```
维度: PMI, 社融, CPI, PPI, M2, DR007, 北向资金
数值: 各项得分 (0~100)
标注: 好坏阈值线
```

### 3.7 MacroView.vue 更新

```
<template> 更新布局:
├── 第一行: MacroScoreGauge (评分仪表盘) + 雷达图
├── 第二行: MacroIndicatorCards (指标卡片，显示单项得分)
├── 第三行: 预警信息 (el-alert，红色标记异常指标)
├── 第四行: MacroTrendChart (趋势图)
└── 第五行: 历史评分折线图
```

---

## 三、MongoDB 索引

```javascript
db.macro_scores.createIndex({ "date": -1 })
```

---

## 四、所需第三方库

| 库 | 用途 | 是否已有 |
|---|------|---------|
| pandas | 分位数计算 | ✅ 已有 |

无需新装。

---

## 五、Phase 3 完成标准

- [ ] 点击"计算评分"能基于当前数据输出 0~100 分
- [ ] 评分仪表盘正确显示分数和等级
- [ ] 各指标贡献度在雷达图中展示
- [ ] 异常指标有红色预警标记
- [ ] 历史评分趋势图可查看
