# Phase 2 - 宏观数据采集

> 目标：能从 AKShare/Tushare/央行官网自动获取 PMI、CPI、PPI、M2、社融、DR007、北向资金、央行公开市场操作等数据，存入 MongoDB。

---

## 一、后端任务

### 2.1 创建宏观数据采集器 `app/services/macro_data_fetcher.py`

```
MacroDataFetcher 类
├── fetch_pmi() → list[MacroIndicator]
│   数据源: AKShare ak.macro_china_pmi()
│   频率: 月度
│   字段: 制造业PMI、非制造业PMI
│
├── fetch_cpi() → list[MacroIndicator]
│   数据源: AKShare ak.macro_china_cpi()
│   频率: 月度
│   字段: CPI同比、CPI环比
│
├── fetch_ppi() → list[MacroIndicator]
│   数据源: AKShare ak.macro_china_ppi()
│   频率: 月度
│   字段: PPI同比、PPI环比
│
├── fetch_m2() → list[MacroIndicator]
│   数据源: AKShare ak.macro_china_money_supply()
│   频率: 月度
│   字段: M2同比
│
├── fetch_social_financing() → list[MacroIndicator]
│   数据源: AKShare ak.macro_china_shrzgm() 或 Tushare pro.shm()
│   频率: 月度
│   字段: 社融存量同比、新增社融
│
├── fetch_dr007() → list[MacroIndicator]
│   数据源: AKShare ak.rate_interbank(money_type="DR007")
│   频率: 日度
│   字段: DR007利率
│
├── fetch_reverse_repo_rate() → list[MacroIndicator]
│   数据源: 央行官网爬虫 或 AKShare
│   频率: 不定期
│   字段: 7天逆回购利率（政策利率）
│
├── fetch_exchange_rate() → list[MacroIndicator]
│   数据源: AKShare ak.currency_boc()
│   频率: 日度
│   字段: 美元兑人民币汇率
│
├── fetch_northbound_flow() → list[MacroIndicator]
│   数据源: AKShare ak.stock_hsgt_north_net_flow_in_em()
│   频率: 日度
│   字段: 北向资金净流入（亿元）
│
└── fetch_open_market_operations() → list[MacroIndicator]
    数据源: 央行官网 RSS (feedparser)
    频率: 每日检查
    字段: 逆回购操作量、MLF操作量、净投放/回笼
```

**统一方法签名**：
```python
async def fetch_and_save(self, indicator_name: str) -> int:
    """采集并入库，返回新增记录数"""
```

**去重逻辑**：按 `{indicator_name, date}` 组合判重，已存在则更新。

### 2.2 央行公开市场操作爬虫

文件：`app/services/crawl_pbc.py`

```
PbcOpenMarketCrawler 类
├── fetch_rss() → list[dict]
│   使用 feedparser 解析央行公开市场操作公告 RSS
│   URL: 中国人民银行官网公开市场业务交易公告
│
├── parse_operation_notice(text: str) → dict
│   解析公告文本，提取：
│   - 操作类型（逆回购/MLF/买断式逆回购）
│   - 操作量（亿元）
│   - 中标利率（%）
│   - 期限（天）
│   - 净投放/净回笼
│
└── parse_latest_rate() → float
    获取当前7天逆回购政策利率
```

**所需库**：
- `feedparser>=6.0.11` - RSS 解析（已有）
- `parsel>=1.10.0` - HTML 解析（已有）
- `re` - 正则提取公告中的数字

### 2.3 在 calendar_service.py 的 MacroDataService 中实现采集逻辑

```python
class MacroDataService:
    def __init__(self):
        self.fetcher = MacroDataFetcher()
        self.pbc_crawler = PbcOpenMarketCrawler()

    async def fetch_indicators(self, names: list[str] = None) -> dict:
        """批量采集指标，返回 {指标名: 新增记录数}"""
        # 如果 names 为空，采集全部指标

    async def get_indicator_history(self, name, start, end) -> list:
        """从 MongoDB 查询历史数据"""

    async def get_latest_indicators(self) -> list:
        """获取每个指标的最新一条"""
```

### 2.4 API 接口实现

在 `app/routers/calendar.py` 中实现：

```
POST /api/calendar/macro/fetch
├── 参数: indicators?: list[str]  (不传则采集全部)
├── 调用 macro_service.fetch_indicators()
└── 返回: { success: true, data: { "PMI": 5, "CPI": 3, ... } }

GET /api/calendar/macro/indicators
├── 调用 macro_service.get_latest_indicators()
└── 返回: [{ indicator_name, value, unit, date, year_on_year, ... }]

GET /api/calendar/macro/indicators/{name}
├── 参数: start_date, end_date, page, page_size
├── 调用 macro_service.get_indicator_history()
└── 返回: 分页列表
```

---

## 二、前端任务

### 2.5 宏观指标卡片组 `frontend/src/components/Calendar/MacroIndicatorCards.vue`

```
布局: Element Plus el-row + el-col，每行4个卡片
每个卡片:
├── 指标名称 (如 "PMI")
├── 最新值 + 单位 (如 "50.4%")
├── 环比变化箭头 (↑ 绿 / ↓ 红)
├── 同比变化
├── 好坏标签 (扩张/收缩 等)
└── 最后更新时间

数据源: calendarApi.macro.getIndicators()
```

### 2.6 指标趋势图 `frontend/src/components/Calendar/MacroTrendChart.vue`

```
ECharts 折线图
├── X轴: 时间
├── Y轴: 指标值
├── 标注线: 好坏阈值 (如 PMI 50 线)
├── 支持切换环比/同比视图
└── 点击指标卡片联动更新图表

数据源: calendarApi.macro.getIndicatorHistory(name)
```

### 2.7 MacroView.vue 页面整合

```
<template> 布局:
├── 顶部操作栏
│   ├── "刷新数据" 按钮 → 调用 calendarApi.macro.fetch()
│   └── 最后更新时间
├── MacroIndicatorCards (指标卡片组)
├── MacroTrendChart (趋势图，选中某个指标时展示)
└── 数据表格 (el-table)
    └── 指标详细历史数据，支持分页
```

---

## 三、MongoDB 索引

```javascript
db.macro_indicators.createIndex(
  { "indicator_name": 1, "date": -1 },
  { unique: true }
)
```

---

## 四、所需第三方库

| 库 | 用途 | 是否已有 |
|---|------|---------|
| AKShare | 宏观数据采集 | ✅ 已有 |
| Tushare | 社融等补充数据 | ✅ 已有 |
| feedparser | 央行 RSS 解析 | ✅ 已有 |
| parsel | 公告 HTML 解析 | ✅ 已有 |
| pandas | 数据处理 | ✅ 已有 |

无需新装任何库。

---

## 五、Phase 2 完成标准

- [ ] 点击"刷新数据"能采集 PMI、CPI、PPI、M2、DR007 等指标
- [ ] 数据存入 MongoDB，重复执行不会产生重复记录
- [ ] 前端指标卡片显示最新值和环比变化
- [ ] 点击指标卡片能展示历史趋势图
